# md_streamlit
A Python Streamlit app hosting analysis and visualisation of the Superstore Marketing Dataset.

To run the app, download the repo, along with Streamlit (pip install streamlit), and run 'streamlit run app.py'.
